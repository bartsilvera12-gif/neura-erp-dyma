import ConsultaPreciosClient from "./ConsultaPreciosClient";

/** Consulta de precios: pantalla de solo lectura para atender al cliente sin exponer datos internos. */
export default function Page() {
  return <ConsultaPreciosClient />;
}
